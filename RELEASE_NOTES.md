* Package Manager: Handle invalid HTTP responses better when accessing packages
* Renderer: Fix various rendering NPEs
* Jekyll: Add the ability to configure the timeout for jekyll (jekyll-timeout = in ig.ini)

